# Getting Started
## Resume
Caféw is a Zumba web application on the Internet written in Javascript using the NodeJS, Express and React frameworks. The data is stored in a MangoDB database.
## Prerequisite
To be able to start the project you are going to need :
 - Git
 - NodeJS
## Installation
Once the prerequisite are installed, follow these steps to install the projet :
 - Clone the project with `git clone https://github.com/Taeith/cafew.git`. 
 - Open a terminal and run`npm install` then `npm start`from the `frontend` folder. 
 - Open a terminal and run`npm install` then `npm start`from the `backend` folder. 
